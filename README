Dependencies:

$ apt install pandoc graphviz highlight

To build the site:

$ soupault

To start a local webserver:

$ python -m http.server --directory build/

To watch for changes and build:

$ ls -d ./**/*.{html, md} | entr sh -c "soupault"